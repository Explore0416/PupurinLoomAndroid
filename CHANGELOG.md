# Changelog

## [0.4.9] - 2026-09-06

- **导入项目：文件被当成目录（EISDIR）（修复）**：`打开作品查看代码` 时报 `script.rpy: open failed: EISDIR`。根因是 SAF/DocumentsProvider 会对某些节点返回错误的 `isDirectory`，把文件当目录处理，落盘成 `<文件同名>/<文件>` 结构。导入改为优先把所选目录解析为**真实文件系统路径**并用 `File.copyRecursively` 直接拷贝（彻底规避提供者的目录误判）；解析不到时（云盘等）才回退到按 uri 去重的 DocumentFile 拷贝。新导入的项目结构不再错乱。
- **返回手势（适配）**：原先是单页应用（内存路由、无浏览器历史），返回键/手势会直接退出。现在返回手势先交给前端——优先调用渲染层注册的 `setNativeBackHandler`，否则自动点击可见的“返回/关闭”按钮回到上一屏；只有确认已在根界面才退出 App。
- **竖屏界面 UI（优化）**：在既有窄屏适配基础上，补充拇指易用性——更大主操作按钮、底部快捷栏贴合手势安全区、弹窗/抽屉贴合竖屏尺寸、减少左右留白、防止表格/面板把页面撑宽。

## [0.4.8] - 2026-09-06

- **导入项目：默认字体 `SourceHanSansLite.ttf` 仍报 `open failed: ENOENT`（彻底修复）**：根因是部分文件选择器会把「同一节点」既作为普通条目、又作为同名目录/自身重复返回。按“名称”去重会在该节点已按文件落盘、再以同名目录出现时，往“文件”里再写同名子项，拼出形如 `…/SourceHanSansLite.ttf/SourceHanSansLite.ttf` 的畸形路径，因父级是文件不是目录而报 `ENOENT`。现将拷贝层改为**按节点 uri 唯一去重**（文件与目录都登记，同一 uri 只拷一次），并加“文件优先、同名空目录清掉”的冲突兜底；读取层路径解析同时自动剥掉重复的 `game/` 前缀与连续同名段。导入/新建项目不再因默认字体报错。

## [0.4.7] - 2026-09-06

- **导入/新建项目：默认字体报错（修复）**：部分文件选择器会把「所选目录自身」作为子节点返回，导致导入时把目录递归拷贝成 `<名字>/<同名>`（如 `SourceHanSansLite.ttf/SourceHanSansLite.ttf`）而报 `open failed: ENOENT`。已为目录拷贝加入“环/自引用/重复名”防护，并让文件读取层的路径解析自动合并重复段，导入项目不再因默认字体报错。

## [0.4.6] - 2026-09-06
- **导入项目更智能（修复）**：现在选中项目「父目录 / 上级文件夹」也能正确识别并导入真正的项目根（自动定位 game/ 或 script.rpy），并保留项目真实名称，不再被错命名为临时目录名。
- **检查更新（修复）**：改为查询本仓库（Explore0416/PupurinLoomAndroid）GitHub Releases 进行真实版本比对，能发现新版本、显示更新详情并直接下载 APK；设置页「在 GitHub 中查看」同步跳转到本仓库。
- **页尾链接**：项目链接改为超链接文本「GitHub项目」，点击在浏览器打开本仓库。
- **固定签名（修复）**：改用仓库内置的固定发布密钥签名，本地与 GitHub Actions 构建的 APK 签名完全一致，后续版本可直接覆盖安装升级。

## [0.4.5] - 2026-09-06

- **云端打包轮询容错（修复）**：轮询过程中手机端的瞬时网络 / DNS 抖动不再被误判为「打包失败」——服务器后台仍在打包，客户端会一直重试直到打包真正完成或超时。

## [0.4.4] - 2026-09-06

- **云端打包网络容错（修复）**：上传 / 轮询 / 下载打包产物时对瞬时网络或 DNS 抖动自动重试，不再把一次网络抖动误判为「打包失败」。

## [0.4.3] - 2026-09-05

- **云端打包产物落地共享目录（修复）**：打包完成的 APK / Web zip 会下载到公共共享目录 `/storage/emulated/0/PupurinLoom/builds/<项目名>/`，可在文件管理器 / 电脑直接访问安装。
- **云端打包设置**：设置面板新增「云端打包」，可在官方打包与自建服务器之间切换，并可编辑自建服务器地址（原生对话框，即时生效并自动保存）。
- **页脚信息**：在“仆仆铃°工作室”下方新增“闪电科技团队进行了安卓版移植 · 项目仓库”及仓库链接。

## [0.4.2] - 2026-09-05

- **存储**：项目数据从私有目录迁移到公共共享目录 `/storage/emulated/0/PupurinLoom/projects/`，Android 11+ 需「所有文件访问」授权，首启引导。
- **云端打包**：内置官方打包服务器 `maker.lightning-team.de5.net`；支持自建服务器（见 `docs/cloud-builder.md`）。
- **竖屏适配**：手机竖屏下不再横向溢出、文字不撑爆宽度。
- **后台行为**：移除画中画(PiP)悬浮窗，按 Home / 切后台正常退到后台。
- **工程化**：接入 GitHub Actions，push 自动构建 APK；打 `v*` 标签自动发布 GitHub Release。

## [0.4.1] - （历史基线）

- 桌面版 [PupurinOfficial/PupurinLoom](https://github.com/PupurinOfficial/PupurinLoom) 的 Android 移植初版：WebView 渲染层 + JS Bridge + 本地化解析，横竖屏 / 小米自由窗口适配、插件系统、运行已导出游戏等。